"""PH-4 model-runtime adapters (Task 4.1 family).

Holds runtime adapter packages (currently the Ollama runtime adapter). Each adapter implements the
routing-owned :class:`~factory.routing.adapters.base.ProviderAdapter` surface.
"""

from __future__ import annotations
